

/*  Installation Steps
        -Create a global install folder in program files for Windows
            -Contains the bin folder with global binaries
            -Also contains the inject.bat file
        -Add the global install bin folder to the PATH permanently
        -Execute registry settings file that runs the 
            autorun batch (containing DOSKEY's) when a console starts
        -
    
*/